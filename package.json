{
  "name": "jarvis-v2",
  "version": "0.1.0",
  "private": true,
  "description": "JARVIS v2 - unified personal AI assistant orchestrator",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build:dashboard && npm run build:orchestrator",
    "build:dashboard": "npm --prefix dashboard install && npm --prefix dashboard run build",
    "build:orchestrator": "tsc -p tsconfig.json",
    "start": "node dist/src/orchestrator/index.js",
    "dev": "tsx src/orchestrator/index.ts",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "seed": "tsx scripts/seed-registry.ts",
    "test": "vitest run",
    "test:dashboard": "npm --prefix dashboard run test",
    "typecheck": "tsc --noEmit && npm --prefix dashboard run typecheck"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@azure/identity": "^4.13.2",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
